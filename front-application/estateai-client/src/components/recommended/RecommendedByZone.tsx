import React from 'react';

import { Typography } from '@material-ui/core';

export default function RecommendedByZone() {
  return (
      <>
        <Typography variant="h3" gutterBottom>
            Here we will have the RecommendedByZone which will let the user to a zone (City/ Zone/ Neighborhood) and see the recomended zone according to the property on google maps
        </Typography>
    </>
  );
}
