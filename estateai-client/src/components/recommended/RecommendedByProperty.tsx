import React from 'react';

import { Typography } from '@material-ui/core';

export default function RecommendedByProperty() {
  return (
      <>
        <Typography variant="h3" gutterBottom>
            Here we will have the RecommendedByProperty which will let the user to choose a property and see the recomended assets according to the property.
        </Typography>
    </>
  );
}
