import { Grid } from '@material-ui/core';
import React from 'react';

import SimpleMap from '../Map/SimpleMap';

export default function SearchPage(props: {bedrooms: string, year: number}) {
    return (
        <Grid> Search page</Grid>
    )
}